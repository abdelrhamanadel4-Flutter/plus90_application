import 'package:flutter/material.dart';

class DeatilsScrean extends StatelessWidget {
  const DeatilsScrean({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("DeatilsScrean ")));
  }
}
